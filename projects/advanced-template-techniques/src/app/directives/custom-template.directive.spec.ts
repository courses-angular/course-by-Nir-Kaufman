import { CustomTemplateDirective } from './custom-template.directive';

describe('CustomTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
